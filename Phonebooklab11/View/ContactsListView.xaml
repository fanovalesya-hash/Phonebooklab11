<UserControl x:Class="Phonebooklab11.View.ContactsListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Phonebooklab11.View"
        mc:Ignorable="d" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!--  Новое: поиск -->
            <RowDefinition Height="Auto"/>
            <!-- Было: Name -->
            <RowDefinition Height="Auto"/>
            <!-- Было: Phone -->
            <RowDefinition Height="Auto"/>
            <!-- Было: кнопки -->
            <RowDefinition Height="*"/>
            <!-- Было: список -->
        </Grid.RowDefinitions>

        <!-- 🔍 Поле поиска (НОВОЕ) -->
        <TextBox Grid.Row="0" Margin="0,5"
             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
             ToolTip="Введите имя или телефон для поиска"/>

        <!-- Имя -->
        <TextBox Grid.Row="1" Margin="0,5"
             Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
             ToolTip="Введите имя контакта"/>

        <!-- Номер -->
        <TextBox Grid.Row="2" Margin="0,5"
             Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
             ToolTip="Введите номер телефона"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10">
            <Button Content="Добавить" Width="100" Margin="0,0,10,0"
                Command="{Binding AddCommand}"/>
            <Button Content="Удалить" Width="100"
                Command="{Binding DeleteCommand}"
                CommandParameter="{Binding SelectedContact}"/>
            <Button Content="Редактировать" Width="100" Command="{Binding EditContactCommand}"/>
        </StackPanel>

        <!-- Список контактов -->
        <DataGrid Grid.Row="4" AutoGenerateColumns="False" IsReadOnly="True"
        ItemsSource="{Binding Contacts}"
        SelectedItem="{Binding SelectedContact}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
